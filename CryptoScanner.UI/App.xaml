<Application x:Class="CryptoScanner.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CryptoScanner.UI"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="AppDarkBackground" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="AppDarkPanel" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="AppDarkAltRow" Color="#252525"/>
        <SolidColorBrush x:Key="AppDarkBorder" Color="#444444"/>
        <SolidColorBrush x:Key="AppDarkText" Color="#F2F2F2"/>
        <SolidColorBrush x:Key="AppMutedText" Color="#B8B8B8"/>

        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource AppDarkBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AppDarkPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AppDarkBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#383838"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#00B8D4"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#125466"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#252525"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#3A3A3A"/>
                                <Setter Property="Foreground" Value="#707070"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource AppDarkPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AppDarkBorder}"/>
            <Setter Property="CaretBrush" Value="{StaticResource AppDarkText}"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource AppDarkPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AppDarkBorder}"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="1" CornerRadius="2">
                                            <ContentPresenter/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <Grid>
                                    <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                      Margin="6,0,28,0" TextElement.Foreground="{TemplateBinding Foreground}"/>
                                    <Path Data="M 0 0 L 8 0 L 4 4 Z" Fill="{TemplateBinding Foreground}"
                                          HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,9,0"/>
                                </Grid>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom"
                                   PopupAnimation="Fade">
                                <Border Background="{StaticResource AppDarkPanel}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                                    <ScrollViewer CanContentScroll="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ToggleButton" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{StaticResource AppDarkPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="{StaticResource AppDarkPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AppDarkBorder}"/>
        </Style>
        <Style TargetType="{x:Type DatePickerTextBox}">
            <Setter Property="Background" Value="{StaticResource AppDarkPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AppDarkBorder}"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="CheckBoxBorder" Width="14" Height="14" Background="#252525"
                                    BorderBrush="#8A8A8A" BorderThickness="1" CornerRadius="2" VerticalAlignment="Center">
                                <Path x:Name="CheckMark" Data="M 2,7 L 6,11 L 12,3" Stroke="#00D9FF"
                                      StrokeThickness="2" Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Margin="6,0,0,0" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#00B8D4"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="CheckBoxBorder" Property="Opacity" Value="0.45"/>
                                <Setter Property="Foreground" Value="#707070"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
            <Setter Property="BorderBrush" Value="#363636"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{StaticResource AppDarkBackground}"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid SnapsToDevicePixels="True">
                            <Border Margin="0,8,0,0" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="2" Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                            <Border HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8,0,0,0"
                                    Padding="4,0" Background="{TemplateBinding Background}">
                                <ContentPresenter ContentSource="Header" RecognizesAccessKey="True"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{StaticResource AppDarkBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AppDarkBorder}"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="{StaticResource AppDarkPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AppDarkBorder}"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1,1,1,0"
                                CornerRadius="2,2,0,0" Padding="10,4" Margin="0,0,2,0">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#333333"/>
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="#555555"/>
                                <Setter TargetName="TabBorder" Property="Panel.ZIndex" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#383838"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="TabBorder" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#333333"/>
                    <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource AppDarkPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
            <Setter Property="RowBackground" Value="{StaticResource AppDarkPanel}"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource AppDarkAltRow}"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource AppDarkBorder}"/>
            <Setter Property="VerticalGridLinesBrush" Value="{StaticResource AppDarkBorder}"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#00D9FF"/>
            <Setter Property="BorderBrush" Value="{StaticResource AppDarkBorder}"/>
            <Setter Property="Padding" Value="5,3"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="{StaticResource AppDarkPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AppDarkBorder}"/>
        </Style>
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{StaticResource AppDarkPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
        </Style>
        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="{StaticResource AppDarkPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource AppDarkText}"/>
        </Style>
    </Application.Resources>
</Application>
